<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_splash_bg_green"
    android:paddingTop="@dimen/splash_fragment_paddingTop"
    tools:context=".splash.ThirdFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/view"
        android:gravity="center">

        <ImageView

            android:id="@+id/splash_cash"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_thumbprint" />

    </LinearLayout>

    <View
        android:id="@+id/view"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/centreViewHeight_splash"
        android:layout_centerInParent="true" />

    <TextView
        android:id="@+id/tv_thirdFragment"
        style="@style/splashFragmentsTextView"
        android:fontFamily="sans-serif-medium"
        android:text="@string/tv_your_data_is_as_safe_as_n_as_your_money" />

    <ImageView
        android:id="@+id/img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_thirdFragment"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/margin_18dp"
        android:src="@drawable/sub_copy_3" />

</RelativeLayout>